//asks user to enter a number and returns it
int readNumber(int cnt);

//prints given integer
void writeAnswer(int result);

//prints given array based on it's size information
void printArray(int [], int size);

//sums the array elements and returns the sum
int sumArray(int [], int size);

//asks user if everything is okay, returns yes or no.
bool askUser();
